export default function LibPage() {
    return (
        <div>
            <h1>This will not be shown in the browser</h1>
            <p>If we use %5F instead of _(underscore) it will be shown as is.<br></br>
            and if we use underscore as a folder name then it will be treated as a private folder.</p>
        </div>
    );
}
